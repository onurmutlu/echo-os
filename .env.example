# ==============================
# 🌌 ECHO.OS — Consciousness Engine
# Environment Configuration File
# ==============================

# === OpenAI API Configuration ===
# Your OpenAI API key (required)
OPENAI_API_KEY=sk-your_api_key_here

# Optional: Organization or project scope if you use them
OPENAI_ORG=
OPENAI_PROJECT=

# === Model Settings ===
# Default OpenAI model (can be any available chat model)
ECHO_MODEL=gpt-4o-mini

# === Database ===
# Local SQLite file — all data (projects, logs, tasks) stored here
ECHO_DB=echo.db

# === Server Configuration ===
# FastAPI host & port for running API
ECHO_HOST=127.0.0.1
ECHO_PORT=8080

# === Logging ===
# DEBUG, INFO, WARNING, ERROR
ECHO_LOG_LEVEL=INFO

# === Security ===
# If you later add a dashboard or web UI
ECHO_SECRET_KEY=c3885d403e2a99f05f7ff3e9157c75b26d6fa531f44b189a8f009ae8bea46295

# === Scheduler (optional, for auto daily cycles) ===
# Set TRUE to enable daily observe/intend jobs
ECHO_SCHEDULER=false

# === Artifacts ===
# Folder to store generated outputs / prompts / images
ECHO_ARTIFACT_DIR=artifacts

# === Environment Metadata ===
# Use 'dev' for local development, 'prod' for deployed container
ECHO_ENV=dev
